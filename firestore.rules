rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public access for warehouse team members across devices without AI Studio account
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
